version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - cd main/app/test
  build:
    commands:
      - npm install
  post_build:
    commands:
      - echo Rig Build Completed in Dev Environment